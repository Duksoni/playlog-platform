To run sqlx migrations, install sqlx-cli cargo package with: `cargo install sqlx-cli`

Command to run migrations: `sqlx migrate add -r <name>`

Run: `cargo sqlx prepare` to cache the queries for macros.