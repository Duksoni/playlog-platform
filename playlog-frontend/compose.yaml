services:
  nginx-server:
    image: playlog/frontend:latest
    container_name: frontend-nginx-server
    build:
      context: .
    environment:
      NODE_ENV: production
    ports:
      - "8080:8080"
    networks:
      - playlog_network
networks:
  playlog_network:
    external: true